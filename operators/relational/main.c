#include <stdio.h>

int main() {
    // in C, comparisons return 1 for true, 0 for false
    printf("comparison %d\n", 5 == 6);
    printf("comparison %d\n", 5 != 6);
    printf("comparison %d\n", 5 > 6);
    printf("comparison %d\n", 5 < 6);
    return 0;
}
